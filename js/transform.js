var transform = {
	init: function(specs){

		if(specs.chart = "all"){
			transform.percentAnnual(specs);
		}
	},
	percentAnnual: function(specs) {
			//
			console.log(specs)
			console.log(specs.flatData[20])
	}
}
